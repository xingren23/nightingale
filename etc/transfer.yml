backend:
  # in ms
  # connTimeout: 1000
  # callTimeout: 3000
  tsdb:
    enabled: false
    cluster:
      tsdb01: 127.0.0.1:5821

  influxdb:
    enabled: true
    username: "influx"
    password: "admin123"
    precision: "s"
    database: "n9e"
    cluster:
      influxdb01: "http://127.0.0.1:8086"

logger:
  dir: logs/transfer
  level: WARNING
  keepHours: 2
